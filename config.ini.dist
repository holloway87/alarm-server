# Copy this file as config.ini to let the server read the configuration.

[DEFAULT]
# WebSocket port
Port = 12614
# The sound file must be placed in the "sound/" directory, and only .wav is supported.
SoundFile = alarm-sound.wav
EnableShutdown = 0
